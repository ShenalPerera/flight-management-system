import {Timestamp} from "rxjs";

export class Route {
  routeID!: number;
  departure!: string;
  destination!: string;
  mileage!: number;
  durationH!: number;
  createdDateTime !: string;
  modifiedDateTime !: string;
  version !: number;


}
